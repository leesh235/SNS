import styled from "../styles/theme-components";

const Wrapper = styled.section``;

export const LogInList = () => {
    return (
        <Wrapper>
            <div>LogInList</div>
        </Wrapper>
    );
};
