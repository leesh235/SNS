export enum Gender {
    MALE = "male",
    FEMALE = "female",
}

export enum Grade {
    ADMIN = "admin",
    USER = "user",
}

export enum LikeStatus {
    LIKE = "like",
    UNLIKE = "unlike",
}

export enum PostMode {
    MY = "my",
    LIKE = "like",
    BOOKMARK = "bookmark",
    FRIENDS = "friends",
    ALL = "all",
}
